# Use provenance-aware Skill Identity

Skillpack will group provider-specific skill instances using provenance-aware Skill Identity: shared real paths, skills.sh metadata, and source identifiers are stronger evidence than names. When provenance is unavailable, Skillpack may fall back to normalized names, but the UI should make inferred grouping visible so users can spot possible false positives.
