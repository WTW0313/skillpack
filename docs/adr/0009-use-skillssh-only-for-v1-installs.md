# Use skills.sh only for v1 installs

Skillpack v1 will install skills only through the skills.sh ecosystem into Global Skills. Arbitrary GitHub installs are out of scope for v1 because they would make Skillpack responsible for repository layout, provenance, update, and lock semantics that are better handled by a dedicated skill ecosystem.
