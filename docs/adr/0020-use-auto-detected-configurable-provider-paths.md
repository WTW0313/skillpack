# Use auto-detected configurable provider paths

Skillpack will auto-detect default provider paths for Codex, Cursor, Claude, Global, and Project Skills, while allowing users to override or add paths in configuration. It will not use a first-run wizard, because the inventory should open quickly for common setups while still supporting custom provider layouts.
